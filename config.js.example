// Local development config — copy this file to config.js and fill in your key.
// config.js is gitignored and must never be committed.
// In production, config.js is generated by the GitHub Actions deploy workflow
// using the AIRTABLE_API_KEY repository secret.
const AT_API_KEY = 'your-airtable-personal-access-token-here';
